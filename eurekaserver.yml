server:
  port: 8070

eureka:
  instance:
    hostname: eurekaserver
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: "http://configserver:8071/eureka"
